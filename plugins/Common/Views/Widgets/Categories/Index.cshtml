@{
    foreach (var item in Model.Categories)
    {
        var cat = item;
        @Html.Raw(string.Format(Model.Template, 
            System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName,
            cat.CategoryUrl, cat.Category, cat.PostCount))
    }
}